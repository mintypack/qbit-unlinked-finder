services:
  qbit-unlinked-finder:
    build: .
    ports:
      - "8000:8000"
    environment:
      - QUF_QBITTORRENT__PASSWORD=${QBIT_PASSWORD}
    volumes:
      - /mnt/user/appdata/qbit-unlinked-finder/config.toml:/config/config.toml:ro
      # One parent mount; hardlinks cannot cross separate bind mounts
      - /mnt/user/data:/data
